import http from 'node:http';import {readFile,stat} from 'node:fs/promises';import path from 'node:path';
const root=path.resolve('dist'),types={'.html':'text/html','.css':'text/css','.mjs':'text/javascript','.webp':'image/webp','.woff2':'font/woff2','.svg':'image/svg+xml','.xml':'application/xml','.txt':'text/plain'};
http.createServer(async(req,res)=>{try{const url=new URL(req.url,'http://localhost');let p=decodeURIComponent(url.pathname);p=p==='/'?'/index.html':p;if(!path.extname(p))p+='.html';const f=path.resolve(root,'.'+p);if(!f.startsWith(root+path.sep))throw Error();const content=await readFile(f);res.writeHead(200,{'Content-Type':types[path.extname(f)]||'application/octet-stream','Cache-Control':'no-cache'});res.end(content)}catch{res.writeHead(404,{'Content-Type':'text/html'});res.end(await readFile(path.join(root,'404.html')))}}).listen(4188,'127.0.0.1',()=>console.log('Catstronaut preview: http://localhost:4188'));
